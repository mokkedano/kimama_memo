<h1>カテゴリ編集</h1>

<%= form_with model: @category, url: category_path, method: :patch, local: true do |f| %>
  <%= f.label :name, "カテゴリ名" %>
  <%= f.text_field :name, placeholder: "20文字以内" %>
  <%= f.submit "変更", class: "btn btn-primary" %>
<% end %>