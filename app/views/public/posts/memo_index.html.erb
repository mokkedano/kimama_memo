<h2>カテゴリ別メモ一覧</h2>

<!--新着順に、カテゴリ関係なくメモ一覧へも飛べるようにする-->

<%# @categories.each do |category| %>
  <%#= link_to category.name, posts_path(category_id: category.id) %>
<%# end %>


<% @category_list.each do |list|%>
  <%= link_to list.name, posts_search_category_path(category_id: list.id) %>
  <%= "(#{list.posts.count})" %>
<% end %>